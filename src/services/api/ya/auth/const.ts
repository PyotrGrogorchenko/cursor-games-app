import { urlRoot } from '../../const'

const urlAuth = `${urlRoot}/auth`

export const url = {
  signup: `${urlAuth}/signup`,
  signin: `${urlAuth}/signin`,
  user: `${urlAuth}/user`,
  logout: `${urlAuth}/logout`
}
