import { urlRoot } from '../const'

const urlUser = `${urlRoot}/user`

export const url = {
  search: `${urlUser}/search`,
  profile: `${urlUser}/profile`,
  password: `${urlUser}/password`,
  avatar: `${urlUser}/profile/avatar`
}
